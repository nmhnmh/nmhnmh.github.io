sudo: required

services:
  - docker

cache:
  directories:
    - .build

before_install:
  - docker pull nmhnmh/sitekicker
  - docker run --rm -v "$TRAVIS_BUILD_DIR:/source" nmhnmh/sitekicker 

script:
  - echo "Done"
