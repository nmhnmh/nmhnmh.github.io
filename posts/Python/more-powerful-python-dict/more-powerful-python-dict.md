Python `dict` is essential, it is used everywhere, it works great, but we want it to do more.

Analyze `collectioins.OrderedDict`.

Object attribute access with dot names
--------------------------------------------------------------------------------
